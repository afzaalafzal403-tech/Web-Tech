Plain HTML CV (no CSS)

Files:

- index.html — plain HTML CV. Open this file in your browser to view the CV.

Usage:

On Windows, double-click `index.html` or open it from your browser. From a terminal you can run:

```powershell
start "" index.html
```

Edit the `index.html` file to customize name, contact, experience, and other sections.
